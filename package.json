{
  "name": "code-iching",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Build step - add your actual build command'",
    "cap:android": "npx cap sync android",
    "android:build": "npm run build && npx cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/cli": "^5.0.0", 
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^2.0.0"
  }
}
