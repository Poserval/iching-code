{
  "name": "code-iching",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p capacitor-web && cp *.html *.css *.js *.json capacitor-web/ && cp -r icons capacitor-web/ && cp -r assets capacitor-web/assets/",
    "clean": "rm -rf capacitor-web android",
    "cap:clean": "rm -rf android && npm run build && npx cap add android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  }
}
