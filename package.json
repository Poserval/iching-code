{
  "name": "code-iching",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "echo 'Building Iching app'",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap add android"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  }
}
