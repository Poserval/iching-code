name: Build APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: ⚙️ Setup environment
      run: |
        npm install -g @capacitor/cli
        npx cap init "Код Ицзин" "com.iching.codeiching" --web-dir .
        npx cap add android
        cd android
        chmod +x ./gradlew
        ./gradlew assembleDebug
